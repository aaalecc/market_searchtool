scraping speed
GUI clean
see current items
reload display
PIL Image
reset tab when clicking out




COMMANDS
    - GIT BRANCH COMMANDS
        git branch "branch_name"
        git checkout "branch_name"
        git checkout "master"
        git merge "branch_name"
    - PYTHON RUN COMMANDS
        python main.py --no-gui
        python main.py --debug
CONFIG:
    - SCRAPING CONFIGS
        BS4
        SELENIUM
    - SETTINGS
        GUI SETTINGS (DEFAULT)

SQL-DATABASE:
    ???????? リサーチ不足

SCRAPERS:
    - GRAILED
    - MERCARI
    - YAHOO AUC
    - YAHOO FLEA
    - SNKRDNK
    - RAKUMA

GUI:
    - SEARCH TAB
    - FEED TAB
    - FAVORITES TAB
    - SETTINGS TAB
        GUI apperance
        webhook-rul linking
        notification settings

NOTIFICATIONS: 
    - DESKTOP NOTI
        plyer library (easy implementaiton)
    - DISCORD NOTI
        webhook-url (GUI settings tab)