いつのスナップショットと比較？（もともとの検索と比較するか、一個前の裏処理と比較するか）
フィード何個まで？（どうやって優先順位決める？）

裏処理と同時にDISCORD通知関数実行




COMMANDS
    - GIT BRANCH COMMANDS
        git branch "branch_name"
        git checkout "branch_name"
        git checkout "master"
        git merge "branch_name"
    - PYTHON RUN COMMANDS
        python main.py --no-gui
        python main.py --debug
CONFIG:
    - SCRAPING CONFIGS
        BS4
        SELENIUM
    - SETTINGS
        GUI SETTINGS (DEFAULT)

SQL-DATABASE:
    ???????? リサーチ不足

SCRAPERS:
    - GRAILED
    - MERCARI
    - YAHOO AUC
    - YAHOO FLEA
    - SNKRDNK
    - RAKUMA

GUI:
    - SEARCH TAB
    - FEED TAB
    - FAVORITES TAB
    - SETTINGS TAB
        GUI apperance
        webhook-rul linking
        notification settings

NOTIFICATIONS: 
    - DESKTOP NOTI
        plyer library (easy implementaiton)
    - DISCORD NOTI
        webhook-url (GUI settings tab)